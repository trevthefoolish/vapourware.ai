name = "vapourware-verse-api"
main = "src/index.js"
compatibility_date = "2024-01-01"

# Development settings
[env.dev]
name = "vapourware-verse-api-dev"

# Production settings
[env.production]
name = "vapourware-verse-api"
routes = [
  { pattern = "api.vapourware.ai", custom_domain = true }
]

# Note: Set your API key as a secret (not in this file!)
# Run: wrangler secret put ANTHROPIC_API_KEY
